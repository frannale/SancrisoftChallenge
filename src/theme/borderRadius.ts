import { normalizeFont } from './normalize';

export const borderRadius = {
  sm: normalizeFont(4),
  md: normalizeFont(8),
  lg: normalizeFont(16),
  xl: normalizeFont(32),
  xxl: normalizeFont(64),
  supreme: normalizeFont(120),
};
